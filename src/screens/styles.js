import { css } from "@emotion/react";

export const feedsContainer = css({
  maxWidth: 980,
  margin: "5em auto 1em",
  display: "flex",
  alignItems: "start",
  flexWrap: "wrap",
  gap: "1rem",
});

export const feedsLine = css({
  flex: "1 1 50rem",
});
